require 'spec_helper'

describe Locomotive::Subtree do
  it 'has a version number' do
    expect(Locomotive::Subtree::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
